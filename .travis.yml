sudo: required

services:
  - docker

before_install:
  - docker image build -t bkouhen/docker-angular-client -f Dockerfile.dev .

script:
  - docker run bkouhen/docker-angular-client npm run test:ci
  - docker run bkouhen/docker-angular-client npm run e2e:ci
